{
  "name": "compass-games-list",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:migrate:deploy": "npm run prisma:migrate:deploy --prefix backend",
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:setup": "npm run install:all && npm run prisma:generate && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
